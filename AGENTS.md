# AGENTS.md

Project guidance for all coding agents lives in [CLAUDE.md](CLAUDE.md). Read it
before making changes.

This file is deliberately a pointer rather than a copy. A second copy drifted
out of date once already: it still described the backend as JavaScript, named
`app.js` and `*.test.js` throughout, and called the TypeScript conversion
unfinished, months after that conversion had landed. One document cannot
disagree with itself.
